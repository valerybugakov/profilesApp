%h2 Sign up
= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
  = devise_error_messages!
  = render "devise/shared/user_fields", f: f, resource: resource

  .actions
    = f.submit "Sign up"

